[project]
name = "esite-tools"
version = "0.1.0"
description = "Python utilities for the eSite repository"
requires-python = ">=3.12"
dependencies = [
    "python-substack",
    "PyYAML",
]

[tool.uv]
package = false
